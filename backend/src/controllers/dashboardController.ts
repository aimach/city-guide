import { Request, Response } from "express";
import { IController } from "./user-controller";

export const DashboardController: IController = {
	getAdmin: async (req: Request, res: Response): Promise<void> => {
		// try {
		// 	//
		// } catch (error) {
		// 	//
		// }
	},
};
